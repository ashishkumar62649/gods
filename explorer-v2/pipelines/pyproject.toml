[project]
name = "god-eyes-pipelines"
version = "0.2.0"
description = "Python data pipelines for God Eyes v2."
requires-python = ">=3.11"
dependencies = [
  "psycopg[binary]>=3.2.0",
  "requests>=2.32.0"
]

[project.scripts]
god-eyes-pipelines = "pipelines.cli:main"
