# Weather Pipeline

Python-first weather intelligence pipeline.

Flow:

```text
fetch -> data_raw -> data_processed -> data_normalized -> PostgreSQL -> API -> frontend
```

Copied JavaScript v1 pipeline files are parked in `explorer-v2/_migration/pending-review/weather-js-pipelines/` until their Python replacements are complete.
