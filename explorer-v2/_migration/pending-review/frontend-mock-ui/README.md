# Frontend Mock UI Pending Review

These files were removed from the active `frontend/src` production path because they were mock-only overlay/rendering utilities that duplicated the Cesium production renderers.

They can be used as visual/reference material only. Reintroduce behavior through `frontend/src/engine`, `frontend/src/earth`, or domain components with backend-backed data and documented layer wiring.
