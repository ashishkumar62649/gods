# Root orchestration environment.
# Copy this to `.env` only if a root-level command needs overrides.

GOD_EYES_DB_PORT=55432
GOD_EYES_DB_PASSWORD=god_eyes_dev_password

# Frontend/backend secrets live in explorer-v2/.env, explorer-v2/frontend/.env,
# or explorer-v2/backend/.env depending on which process consumes them.
